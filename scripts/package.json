{
  "name": "@workspace/scripts",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed-wineries": "tsx ./src/seed-wineries.ts",
    "seed-restaurants": "tsx ./src/seed-restaurants.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
